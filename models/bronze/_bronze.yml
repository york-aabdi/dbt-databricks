version: 2

models:
  - name: bronze_customers
    columns:
      - name: customer_id
        tests: [not_null, unique]

  - name: bronze_orders
    columns:
      - name: order_id
        tests: [not_null, unique]
